query featuredPosts($count: Int!) {
  featuredPosts(count : $count) {
    data {
      id
      title
      slug
      excerpt
      image_url
    }
    paginatorInfo {
      count
      currentPage
      firstItem
      hasMorePages
      lastItem
      lastPage
      perPage
      total
    }
  }
}
