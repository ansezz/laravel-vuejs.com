{
  "name": "laravel-vuejs",
  "version": "0.1.0",
  "scripts": {
    "dev": "nuxt --prefix front",
    "build": "nuxt build --prefix front",
    "start": "nuxt start --prefix front",
    "postinstall": "npm install --prefix front",
    "heroku-postbuild": "npm run build --prefix front"
  }
}